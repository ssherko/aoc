import helpers

DAY = 7
PART = 2


def solution(f_obj):
	# same as pt1, only the text file changes 
	# NOTE 'make runall` prints `!NOT IMPLEMENTED!` as an answer for this, which is not the case.
	pass

helpers.solve(DAY,PART,solution,line_by_line=True)